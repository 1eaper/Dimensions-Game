package game;

import game.engine.Draw;
import game.engine.GameObject;

import org.lwjgl.opengl.Display;

public class Game {

	private Player player;
	
	public Game() {
		
		player = new Player(50f, 50f, 50f);
	}
	
	public void getInput() {
		
		
	}
	
	public void update() {
		
		
	}
	
	public void render() {
		
		Draw.drawCir(Display.getWidth() / 2 - 25, Display.getHeight() / 2 - 25, 50);
	}
}
